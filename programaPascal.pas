program condicional;
var x, y : integer;
    z : integer;
begin
    read(x,y);
    write(x);
    writeln(x);
    for i := to do 
        begin
            writeln(x);
        end;
    repeat
        read(x);
    until();
    while() do
        begin
            read(x);
        end;
    if() then
        begin
            read(x);
        end;
    if() then
        begin
            read(x);
        end
    else
        begin
            read(x);
        end;
end.